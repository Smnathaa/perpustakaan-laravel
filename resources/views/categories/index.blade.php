@extends('layouts.app')
@section('title','Kategori')
@section('content')<div class="heading"><div><span class="eyebrow">ORGANISASI KOLEKSI</span><h1>Kategori buku</h1><p>Kelompokkan buku agar lebih mudah ditemukan.</p></div></div><form class="filters" method="POST" action="{{ route('categories.store') }}">@csrf<label class="search">Nama kategori baru<input name="name" value="{{ old('name') }}" placeholder="Contoh: Teknologi" required maxlength="255"></label><button class="btn">+ Tambah kategori</button></form><section class="panel"><div class="table-wrap"><table><thead><tr><th>NAMA KATEGORI</th><th>JUDUL BUKU</th><th>AKSI</th></tr></thead><tbody>@forelse($categories as $category)<tr><td><form id="category-{{ $category->id }}" class="inline-form" action="{{ route('categories.update',$category) }}" method="POST">@csrf @method('PUT')<input aria-label="Nama kategori {{ $category->name }}" name="name" value="{{ $category->name }}" required maxlength="255"></form></td><td>{{ $category->books_count }}</td><td><div class="actions"><button class="text-btn" form="category-{{ $category->id }}">Simpan</button><form method="POST" action="{{ route('categories.destroy',$category) }}" data-confirm="Hapus kategori ini?">@csrf @method('DELETE')<button class="text-btn danger">Hapus</button></form></div></td></tr>@empty<tr><td colspan="3" class="empty">Belum ada kategori.</td></tr>@endforelse</tbody></table></div></section>@endsection
